# This is a template for Streamlit secrets (stored securely on Streamlit Cloud)
# On your local machine, create .streamlit/secrets.toml with these values:
# On Streamlit Cloud, go to App settings → Secrets and paste the content below

backend_url = "http://localhost:8000"
# For Streamlit Cloud, change to your deployed Django backend URL, e.g.:
# backend_url = "https://your-django-app.render.com"
